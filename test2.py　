from flask import Flask, render_template

app = Flask(__name__)

@app.route('/')
def index():
    # ここで "hello" という文字（文字列）を返しているので、
    # 実際には render_template は使われていませんが、動作に問題はありません！
    return "hello"

if __name__ == '__main__':
    # ローカル（自分のPC）でテストする時のための設定
    app.run()
